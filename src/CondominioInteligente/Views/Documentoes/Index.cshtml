@model IEnumerable<CondominioInteligente.Models.Documento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Arquivo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reuniao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Arquivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reuniao.Pauta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodDocumento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodDocumento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodDocumento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
